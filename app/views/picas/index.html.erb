

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="container">
    
    <div class="navbar-header">
      <a class="navbar-brand" href="#"><i class="fa fa-instagram fa-lg">Instagram</i></a>
    </div>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#"><i class="fa fa-compass fa-lg fa-2x"></i></a></li>
        <li><a href="#"><i class="fa fa-heart-o fa-lg fa-2x"></i></a></li>
        <li><a href="#"><i class="fa fa-user-o fa-lg fa-2x"></i></a></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
</div> <!--container-->
<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="col-xs-8 col=xs-offset-4">
      <div class="panel panel-default">
        <div class="panel-body">
          <i class="fa fa-plus-circle fa-5x"><%= link_to('Edit Profile', edit_user_registration_path) %></i>
          <p>Add a Profile Photo</p>
        </div>
      </div>
    </div>
    <div class="col-xs-8 col=xs-offset-4">
      <div class="panel panel-default">
        <div class="panel-body">
        <%= image_tag(current_user.avatar.url(:thumb)) %>
         <p><%= current_user.username %></p>
            
        </div>
        <div class="panel-footer">
          <p><%= current_user.username %></p>
        </div>
      </div>
    </div>
  </div>
</div>
<%= link_to('Logout', destroy_user_session_path, method: :delete) %>
<%= current_user.fullname %>
<%= current_user.username %>
<h1>Listing Picas</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Picture</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @picas.each do |pica| %>
      <tr>
        <td><%= pica.name %></td>
        <td><%= pica.description %></td>
        <td><%= pica.picture %></td>
        <td><%= link_to 'Show', pica %></td>
        <td><%= link_to 'Edit', edit_pica_path(pica) %></td>
        <td><%= link_to 'Destroy', pica, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pica', new_pica_path %>
